# Copy to .env and fill in values locally. Never commit .env files.
#
# The current AI chat uses on-device deal data only (no external LLM).
# Add these only if you wire up live providers later:

# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# COSTAR_API_KEY=
# REALPAGE_API_KEY=
